#!/bin/sh

D=`date`
echo "[$D] locking"

# kill any active tokens
doas -L
ssh-add -D

~/bin/keepalive disable

# clear clipboard
echo -n | pbcopy

~/bin/music pause
playerctl pause firefox
pkill -9 vlc
slock
